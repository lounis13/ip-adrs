> Pour execution : 

    - Methode 01 : (Voir Makefile)
        make (ou make run )pour executer .  
        make (compile) pour compiler .   
        
    - Methode 02 : 
        placez vous dans src/ : 
        ../src/ $ javac *.java  ( compil )
        ../src/ $ java   DnsApp (executer)

    - Methode 03 : 
        intelliJ ou  eclipse ...

ps : 

    * pour les deux fichier BDD.txt et fic.properties ils sont dans le dossier pere
      et le dossier src/ (par ce que ca depend de l'execution de pgrm ) 
         - si avec make ou(intelliJ ...)il chage les deux fichers de dossier pere
         - si avec javac *.java les fichier class ce cree dans src donc il charge
           les deux fichiers de src/   
